# Revision Notes

- basedOnDraft: yes
- changes applied: Translated figure captions while preserving embedded media and code. Added explicit translator notes for the source's 78/77 example-count discrepancy and 13%/20% CPU figures. Removed an unsupported currency unit. Documented the interactive canvas limitation with a direct original-site link. Preserved all recovered sections, table cells, anchors, code, and SVG figures.
- unresolved issues: No unresolved material translation or structure issues. The interactive canvas cannot run without the original site's scripts; the caption explicitly directs readers there. Numerical inconsistencies remain disclosed source limitations, not silently corrected facts.
- scope: Only this generated article directory was repaired; shared scripts were not changed. An invisible end-of-article comment prevents the apply script's unconditional closing-fence removal from deleting the final TLA+ fence.
- verification: Source-to-final audit passed for 72 headings, 42 byte-identical fenced code blocks, all 33 figures, 11 exact SVG assets, unchanged media tags, and original link targets. Repository validation passed: 41 tests and a 72-page Astro build.
